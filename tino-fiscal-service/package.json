{
  "name": "@tino/fiscal-service",
  "private": true,
  "version": "0.1.0",
  "description": "Isolated fiscal document discovery service for TINO",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "check": "node --experimental-strip-types --check src/types.ts && node --experimental-strip-types --check src/service.ts && node --experimental-strip-types --check src/server.ts && node --experimental-strip-types --check src/nfewizard-io.ts",
    "preflight": "node --experimental-strip-types scripts/preflight.ts"
  }
}
